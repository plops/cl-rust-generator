[workspace]
members = [
    "proto-definitions",
    "cloud-proxy-server",
    "minimal-tui-client",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
