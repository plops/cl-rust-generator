[workspace]
members = [
    "proto-def",
    "core-utils",
    "cloud-render-srv",
    "macroquad-client",
]
resolver = "2"

[profile.dev]
debug = 1
incremental = true

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
