[package]
name = "proto-def"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
serde = { version = "1.0", features = ["derive"] }

[build-dependencies]
tonic-build = { version = "0.12", features = ["prost"] }
